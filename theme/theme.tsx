import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'Grumpy Black 48', san-serif`,
    body: `'Basier Square', sans-serif`,
  },
});

export default theme;
